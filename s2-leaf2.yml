---
network_ports:
  - switches:
      - s2-leaf2
    switch_ports: Ethernet6
    enabled: true
    mtu: 9000
    profile: int_routed_host
    structured_config:
      ip_address: 10.192.226.254
      vrf: bluevrf
      description: test eth6 routed port
      mtu:
